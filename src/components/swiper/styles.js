import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
  },

  pager: {
    position: 'absolute',
    bottom: 0,
    left: 0,
    right: 0,
  },
});
